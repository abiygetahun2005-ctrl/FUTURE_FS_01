const LanguageToggle = () => {
  return (
    <button className="text-sm font-body font-medium text-muted-foreground hover:text-foreground transition-colors">
      EN
    </button>
  );
};

export default LanguageToggle;